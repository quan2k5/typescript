"use strict";
let firstName = 'lê';
firstName = firstName.replace(firstName[0], firstName[0].toUpperCase());
let secondName = 'quân';
secondName = secondName.replace(secondName[0], secondName[0].toUpperCase());
let fullName = firstName + ' ' + secondName;
console.log(fullName);
